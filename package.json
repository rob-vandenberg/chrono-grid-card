{
  "name": "chrono-grid-card",
  "version": "1.0.0",
  "scripts": {
    "build": "terser src/chrono-grid-card.js -o dist/chrono-grid-card.js --compress --mangle"
  },
  "devDependencies": {
    "terser": "^5.0.0"
  }
}
